<%= form_for(@courier_session) do |f| %>
  <% if @courier_session.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@courier_session.errors.count, "error") %> prohibited this courier_session from being saved:</h2>

      <ul>
      <% @courier_session.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :username %><br />
    <%= f.text_field :username %>
  </div>
  <div class="field">
    <%= f.label :password %><br />
    <%= f.password_field :password %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
